// Paste your Firebase web-app config here (Firebase console → Project settings → Your apps → SDK config).
// null = app runs local-only (no sync). The apiKey is not a secret; Firestore rules protect the data.
export const firebaseConfig = null;
/* example:
export const firebaseConfig = {
  apiKey: "...", authDomain: "xxx.firebaseapp.com", projectId: "xxx",
  storageBucket: "xxx.appspot.com", messagingSenderId: "...", appId: "..."
};
*/
